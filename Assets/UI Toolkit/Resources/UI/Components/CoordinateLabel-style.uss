.coordinate-label {
    flex-shrink: 0;
    flex-grow: 0;
    flex-direction: row;
}

.coordinate-label > * {
    padding: 0;
    margin: 0;
}

.coordinate-label__axis {
    -unity-font-definition: var(--nl3d-header-font);
}

.coordinate-label__value {
    padding-left: var(--spacing-1);
}
