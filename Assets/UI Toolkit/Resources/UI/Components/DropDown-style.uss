.dropdown {
    height: var(--spacing-11);
    background-color: transparent;
    border-radius: var(--spacing-4);
    border-width: var(--spacing-1);
    border-color: var(--color-blue-50);
    padding: var(--spacing-2);
    margin: 0;
    flex-direction: row-reverse;
    justify-content: space-around;
    align-items: center;
    flex-shrink: 1;
    flex-grow: 0;
}

.dropdown:hover {
    border-color: var(--color-blue-700);
}

.dropdown .unity-base-field__input {
    color: var(--color-white);
    background-color: transparent; 
    margin: 0;
    padding: 0;
    border-width: 0;
    min-width: 0;
}

.dropdown .icon {
    background-image: var(--icon-keytokencode);
}

.dropdown .unity-base-popup-field__arrow {
    background-image: var(--icon-chevrondown);
    width: var(--spacing-6);
    height: var(--spacing-6);
    margin-left: var(--spacing-2);
}

.dropdown .unity-text-element {
    display: none;
}

.dropdown-popup-item {   
    background-color: white;
    border-color: var(--color-blue-700);
    border-radius: var(--spacing-4);
    border-width: var(--spacing-1);
    padding: var(--spacing-2);
    margin: 0;
    align-items: center;
    flex-shrink: 1;
    flex-grow: 0;
}

.dropdown-popup-item.dropdown-popup-item__first-item {
    border-top-width: var(--spacing-1);
    border-bottom-width: 0;
    border-top-left-radius: var(--spacing-4);
    border-top-right-radius: var(--spacing-4);
    border-bottom-left-radius: 0;
    border-bottom-right-radius: 0;
    margin-bottom: 0;
}

.dropdown-popup-item.dropdown-popup-item__last-item {
    border-top-width: 0;
    border-bottom-width: var(--spacing-1);
    border-top-left-radius: 0;
    border-top-right-radius: 0;
    border-bottom-left-radius: var(--spacing-4);
    border-bottom-right-radius: var(--spacing-4);
    margin-top: 0;
}

.dropdown-popup-item.dropdown-popup-item__middle-item {
    border-top-width: 0;
    border-bottom-width: 0;
    border-radius: 0;
    margin: 0;
}


.dropdown-popup-item:hover {    
    background-color: var(--color-blue-700);
}

.dropdown-popup-item:hover .icon{
    -unity-background-image-tint-color: var(--color-white);
}

.dropdown-popup-container .unity-repeat-button,
.dropdown-popup-container .unity-scroll-view__vertical-scroller,
.dropdown-popup-container .unity-scroll-view__horizontal-scroller,
.dropdown-popup-container .unity-scroller__slider,
.dropdown-popup-container .unity-scroller {
    width: 0;
    height: 0;
    min-width: 0;
    min-height: 0;
    opacity: 0;
    display: none;
}

.dropdown-popup-container > * {
    padding: 0;
    background-color: transparent;
}